import useBitcoinApi from "../Hooks/useBitcoinApi";

export default function BitcoinBarGraph() {
   
    return (
        <div className="barGraphBar" style={{ height: `${height}px` }}>
          {/* Your content here */}
        </div>
      );
    }
}